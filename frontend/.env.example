# Local development only — copy this file to .env.local and adjust if
# your backend runs elsewhere. For a deployed frontend (e.g. Vercel),
# set VITE_API_BASE_URL as an environment variable on the hosting
# platform instead — see frontend/README.md's Deployment section.
VITE_API_BASE_URL=http://127.0.0.1:8000
